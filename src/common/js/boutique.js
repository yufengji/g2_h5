export default class boutique{
  constructor({id,icon,name,slogan,type,flag}){
    this.id = id
    this.icon = icon
    this.name = name
    this.slogan = slogan
    this.type = type
    this.flag = flag
  }
}
